# NoteCraft
A PWA note-taking web app built with PHP, MySQL, and JavaScript featuring authentication, CRUD notes, labels, image upload, real-time editing (Socket.io), auto-save, offline support (IndexedDB + Service Worker), sync on reconnect, sharing with permissions (read, edit), password protection, theming, and cloud deployment with HTTPS.
